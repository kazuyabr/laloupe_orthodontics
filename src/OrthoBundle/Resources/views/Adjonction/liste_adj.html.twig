{# @OrthoBundle/Resources/views/Default/liste_adj.html.twig #}
{% extends 'base.html.twig' %}
{% block body %}
        <div id="msform">
            {% if app.user %} <!-- L'utilisateur est connecté -->
                <fieldset class="largebloc">
                    <h2 class="fs-title">Liste des adjonctions</h2>

                {% for adjonctions in adjonctions %}

                    <a id="liste"
                       href="{{ path('fiche_adj', { 'id': adjonctions.id }) }}">{{ adjonctions.id }}  {{ adjonctions.nom }}</a>
                    <br/>
                    <a id="lienedit" href="{{ path('fiche_adj', { 'id': adjonctions.id }) }}">Voir</a> &nbsp;
                    <a id="lienedit" href="{{ path('edit_adj', { 'id': adjonctions.id }) }}">Editer</a>
                    <br/>

                {% endfor %}


                </fieldset>
            {% else %} <!-- L'utilisateur n'est pas connecté -->
                <fieldset class="smallbloc">
                    Connectez-vous pour accéder à cette page.
                </fieldset>
            {% endif %}
        </div>
{% endblock %}